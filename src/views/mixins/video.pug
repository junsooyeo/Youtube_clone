mixin video(video)
    div
        h4
            a(href=`/videos/${video.id}`)=video.title
    ul
        li rating: #{video.rating}/5
        li comments: #{video.comments} comments
        li created: #{video.createdAt}
        li views: #{video.views}
        li id : #{video.id} 